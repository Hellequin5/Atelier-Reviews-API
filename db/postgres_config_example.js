const config = {
  user: "FILL_ME_IN",
  host: "localhost",
  database: "FILL_ME_IN",
  password: "FILL_ME_IN",
  port: "5432"
}

// You may need to edit the host or port depending on your local setup, but this is the root setup for postgres

module.exports = config;